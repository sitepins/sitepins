name: build
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable
      - run: pnpm install
      - name: Build-time env for the web app
        run: |
          printf '%s\n' \
            'NEXT_PUBLIC_BACKEND_URL=https://sitepins-app.salite.top' \
            'NEXT_PUBLIC_HP_WS_URL=wss://sitepins-app.salite.top/api/v1/editor/collab' \
            > app/.env
      - run: pnpm -r build
      - uses: actions/upload-artifact@v4
        with:
          name: sitepins-build
          include-hidden-files: true
          path: |
            app/.next
            api/dist
