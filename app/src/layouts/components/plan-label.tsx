"use client";

// Plan name shown under the org name in the org switcher. The open-source
// build has no plans, so nothing is rendered. The hosted cloud edition
// overrides this module (plan-label.cloud.tsx).

export function PlanLabel(_props: { activePackage?: string }) {
  return null;
}
