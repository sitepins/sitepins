"use client";

// Upgrade action shown in the site-quota dialog. Unreachable in the
// open-source build (no quotas). The hosted cloud edition overrides this
// module (quota-upgrade-action.cloud.tsx).

export function QuotaUpgradeAction() {
  return null;
}
