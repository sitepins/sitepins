"use client";

// Sidebar upgrade button. Not rendered in the open-source build — there is
// nothing to upgrade to. The hosted cloud edition overrides this module
// (sidebar-upgrade.cloud.tsx).

export function SidebarUpgrade() {
  return null;
}
