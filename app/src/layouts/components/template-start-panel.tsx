"use client";

// "Start with a Template" panel in the Add New Site dialog. The template
// catalog is a hosted-cloud feature — the open-source build imports sites
// from your own GitHub/GitLab repos, so this renders nothing here. The
// cloud edition overrides this module (template-start-panel.cloud.tsx).

export const hasTemplatePanel = false;

export function TemplateStartPanel() {
  return null;
}
