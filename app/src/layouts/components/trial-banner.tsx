"use client";

// No trials in the open-source build. The hosted cloud edition overrides
// this module (trial-banner.cloud.tsx) with the real banner.

export function TrialBanner() {
  return null;
}
